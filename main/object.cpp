#include "Object.h"

Object::Object(Vector2Int pos, Vector2Int velocity, int width, int height, bool isCircle, Note note) : pos(pos), velocity(velocity), width(width), height(height), isCircle(isCircle), note(note){

}